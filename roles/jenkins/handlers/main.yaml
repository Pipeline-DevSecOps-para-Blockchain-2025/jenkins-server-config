- name: Reload nginx
  ansible.builtin.service:
    name: nginx
    state: reloaded

- name: Reload user systemd
  ansible.builtin.systemd_service:
    daemon_reload: true
    scope: user
  become: true
  become_user: runner
  become_method: su
  become_flags: '-l'

- name: Restart jenkins
  ansible.builtin.systemd_service:
    name: jenkins
    state: restarted
    scope: user
  become: true
  become_user: runner
  become_method: su
  become_flags: '-l'
