- name: Restart serial consoles
  ansible.builtin.systemd_service:
    name: '{{ item }}'
    state: restarted
    daemon_reload: true
  loop: '{{ ansible_facts.services.keys() | select("match", "serial-getty@.+\\.service") | list }}'
